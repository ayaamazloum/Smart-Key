import 'package:flutter/material.dart';

class KncockScreen extends StatelessWidget {
  const KncockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}